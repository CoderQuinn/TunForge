# TunForge 0.0.1 (Demo)

Minimal demo for the TunForge pipeline (TUN simulation → stack → redirect to local server).

Build & run:
1. swift build
2. swift run TunForgeDemo

This demo simulates TUN with a timer; it redirects payloads to local TCP server at 127.0.0.1:1208.
