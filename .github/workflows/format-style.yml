name: format

on:
  push:
    branches: ["*"]
  pull_request:

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install clang-format
        run: sudo apt-get update && sudo apt-get install -y clang-format

      - name: Check clang-format (ObjC/C)
        run: |
          find Sources/TunForgeCore -type f \( -name "*.m" -o -name "*.mm" -o -name "*.h" -o -name "*.c" -o -name "*.cpp" \) -print0 | xargs -0 clang-format -i
          git diff --exit-code
